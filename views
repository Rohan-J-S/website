from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.
def home(request):
    return HttpResponse("""this is the home page of the web application \n
    This in a webpage made on django""")
